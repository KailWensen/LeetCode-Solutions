class Solution {
    public int arraySign(int[] nums) {
        int counter = 1;
        for(int i : nums)
        {
            if(i<0)
                counter *=-1;
            else if(i>0)
                counter*=1;
            else
                counter*=0;
        }

        return counter;


    }
}
